name: update
on: [workflow_dispatch]
jobs:
  update:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo %CD%
      - run: where curl
      - run: where cscript
      - run: where msxsl
